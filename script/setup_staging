# remove backup script
sudo rm /root/.backup_config

# change local_settings to allow 'stage.ona.io' as host
echo "ALLOWED_HOST = ['stage.ona.io']" >> ~/src/ona/onadata/settings/local_settings.py

# prefix site_name with '[STAGE]'
